<%= form_for(@classroom) do |f| %>
  <% if @classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :classroom_description %><br>
    <%= f.text_field :classroom_description %>
  </div>
  <div class="field">
    <%= f.label :classroom_status_id %><br>
    <%= f.number_field :classroom_status_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
